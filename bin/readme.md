This file is a placeholder to create the `bin` directory for building the target.

`exampleConfig.json` is a ready-to-edit test configuration (Kadaster
open caster, ephemeris stream included); copy it to `config.json` and
substitute your own free Kadaster registration for the placeholder
credentials. Everything else in this directory is build output and is
not tracked.
