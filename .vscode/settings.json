{
    "code-runner.executorMap": {
        "cpp": "gcc -std=c++17 -Wall -O2 -o output $fileName lib/cJSON/cJSON.c -lws2_32 && ./output",
        "c": "gcc -Wall -O2 -o output $fileName lib/cJSON/cJSON.c -lws2_32 && ./output"
    },
    "code-runner.fileDirectoryAsCwd": true // Ensure the working directory is set to the file's directory
}
